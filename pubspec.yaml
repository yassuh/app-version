name: app_version
publish_to: none
description: This is a Flutter widget that checks and displays the version status of application and you can easily guide user to update your app.
version: 0.0.2
homepage: https://github.com/myConsciousness/app-version

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  new_version: 
    git: 
      url: https://github.com/yassuh/new_version.git
  package_info_plus: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  import_sorter: ^4.6.0
